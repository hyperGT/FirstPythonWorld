# Programa que lê um número, e retorna o seu antecessor e o seu sucessor

print('Find the predecessor and the successor of a number ---')

number = int(input('enter the number: '))

predecessor_number = number-1
successor_number = number+1

print('\n Number: {} \n predecessor: {} \n successor {}'.format(number, predecessor_number, successor_number))

