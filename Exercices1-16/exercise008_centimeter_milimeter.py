# convert meters in centimeters and milimeters

m = float(input('Enter lengths in meters to convert: '))
c = m * 100  # centimeters
mi = m * 1000  # milimeters

print(f'lenght: {m} \n centimeter: {c} \n milimeter: {mi}')
